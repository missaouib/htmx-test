{
  "name": "htmx-test-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo \"This is a Thymeleaf template. Build first then run the Spring Boot project.\" && vite",
    "build": "tsc && vite build",
    "watch": "vite build --watch",
    "preview": "echo \"This is a Thymeleaf template. Build first then run the Spring Boot project.\" && vite preview"
  },
  "devDependencies": {
    "@shoelace-style/shoelace": "^2.7.0",
    "autoprefixer": "^10.4.15",
    "htmx.org": "^1.9.4",
    "lit": "^2.8.0",
    "postcss": "^8.4.29",
    "tailwindcss": "^3.3.3",
    "typescript": "^5.0.2",
    "vite": "^4.4.5",
    "vite-plugin-static-copy": "^0.17.0"
  }
}
